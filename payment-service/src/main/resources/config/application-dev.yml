spring:
  datasource:
    url: jdbc:postgresql://payment-db:5432/${PAYMENT_DB_NAME}
    username: ${PAYMENT_DB_USER}
    password: ${PAYMENT_DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

user-service:
  url: user-service:8083

meeting-service:
  url: meeting-service:8088

tutoring-service:
  url: tutoring-service:8085

application:
  url: eduvy.pl